# Server Configuration
spring.application.name=flash-mind-backend
server.port=${PORT:8080}

# Database Configuration - PostgreSQL Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d4d2ts0gjchc73do1cs0-a.frankfurt-postgres.render.com:5432/flash_mind_db}
spring.datasource.username=${DB_USERNAME:flash_mind_db_user}
spring.datasource.password=${DB_PASSWORD:nUY5gKopN0jxTc2mB44ZaDNHYnopCT7r}

# JPA Configuration - CORRIGÉ pour PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:flashmindquizz@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:mfwf gwxc dvlc mims}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Désactiver temporairement la vérification email
app.email.verification.enabled=false

app.base-url=${APP_BASE_URL:http://localhost:8080}
app.frontend-url=${FRONTEND_URL:http://localhost:5173,https://flashmindquiz.netlify.app}

# JWT Configuration
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,https://flashmindquiz.netlify.app}

# Logging
logging.level.com.flashmind=DEBUG
logging.level.org.springframework.security=DEBUG

# Encoding fix
spring.messages.encoding=UTF-8
server.servlet.encoding.charset=UTF-8