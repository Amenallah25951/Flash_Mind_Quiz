spring:
  application:
    name: flash-mind-backend
  profiles:
    active: prod

server:
  port: 8080

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://mysql-amenallah.alwaysdata.net:3306/amenallah_flash_mind_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: amenallah
    password: Amen20/12/2004/123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 5
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: update  # ⚠️ CHANGÉ de create-drop à update pour ne pas perdre les données
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        jdbc:
          time_zone: UTC

  # ✅ CONFIGURATION EMAIL AVEC PORT 465 SSL (pour Render)
brevo:
  api-key: ${BREVO_API_KEY}

app:
  email:
    verification:
      enabled: true
  base-url: https://flash-mind-quiz-10.onrender.com
  frontend-url: https://flashmindquiz.netlify.app

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

cors:
  allowed:
    origins: https://flashmindquiz.netlify.app

logging:
  level:
    com.flashmind: DEBUG
    org.springframework.security: DEBUG
    org.springframework.mail: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always